gRPC MATCH — @grpc/grpc-js runs on this engine as both peers, and every frame crosses to real nghttp2: node's client made 4 exchanges against this engine's server, and this engine's client made 4 against node's — unary, initial metadata, a server-streaming call over one stream, and a rejected call whose status arrives in the trailers
