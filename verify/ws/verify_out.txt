installing ws with our own package manager…
ws SERVER on our engine — a real node ws client cannot tell it from node's:
open
message welcome
message echo:one
message echo:two
close; saw 3 messages
timeout
ws CLIENT on our engine — a real node ws server cannot tell it from node's
WS: ALL PASS
