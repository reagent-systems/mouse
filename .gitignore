# macOS
.DS_Store

# Xcode
build/
DerivedData/
xcuserdata/
*.xcuserstate

# Android / Gradle
.gradle/
.kotlin/
kotlin/local.properties
kotlin/app/build/
*.apk

# Leftover tooling
node_modules/

# Secrets & local config
.env
.env.*
.claude/settings.local.json

# Ralph-loop runtime state: an iteration counter that changes on every tick,
# and agent worktrees. afca53e removed the loop file from tracking once already.
.claude/ralph-loop.local.md
.claude/worktrees/

# Lighthouse CI local reports
.lighthouseci/
